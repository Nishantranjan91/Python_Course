import math

# Input radius from user
radius = float(input("Enter the radius of the hemisphere: "))

# Calculate total surface area of hemisphere
tsa = 3 * math.pi * radius ** 2

# Display result
print("Total Surface Area of the Hemisphere =", tsa)